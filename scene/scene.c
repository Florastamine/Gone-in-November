/*
 * <scene>
 * 
 * This is a simple code fragment which (along with scene.wdl), forces the .wmp files to recognize the
 * code and assets outside of this folder. Must be assigned with every scene file.
 */
#define PRAGMA_PATH "../"

#include "../November.h"
